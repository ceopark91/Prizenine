import Link from 'next/link';
import type { Product } from '@/db/schema';
const marketNames: Record<string, string> = { coupang: '쿠팡', temu: '테무', aliexpress: '알리익스프레스' };
const statusNames: Record<string, string> = { draft: '준비 중', queued: '제작 대기', generating: '영상 제작 중', ready: '영상 준비 완료', failed: '제작 실패' };
export function ProductCard({ product }: { product: Product }) { return <Link href={`/p/${product.code}`} className="group overflow-hidden rounded-[1.35rem] border border-[var(--border)] bg-white shadow-[0_12px_40px_rgba(28,42,76,.06)] transition hover:-translate-y-1 hover:shadow-[0_18px_50px_rgba(28,42,76,.12)]"><div className="relative aspect-[4/3] overflow-hidden bg-[var(--muted)]"><img src={product.imageUrl || '/favicon.svg'} alt="" className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /><div className="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1 text-xs font-bold text-[var(--primary)]">{product.code}</div></div><div className="p-5"><div className="mb-2 flex items-center justify-between gap-3 text-xs text-[var(--muted-foreground)]"><span>{marketNames[product.marketplace] ?? product.marketplace}</span><span>{statusNames[product.status] ?? product.status}</span></div><h2 className="display text-xl font-bold leading-tight">{product.name}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-[var(--muted-foreground)]">{product.description}</p></div></Link>; }
